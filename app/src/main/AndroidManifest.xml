<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testapiipidymethods">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestApiIpidyMethods"
        android:usesCleartextTraffic="true">
        <activity android:name=".IDS.Actions.VerifyActivity"></activity>
        <activity android:name=".IDS.Actions.CreateAccountActivity" />
        <activity android:name=".CameraJavaActivity" />
        <activity android:name=".IDS.CameraPreviewActivity" />
        <activity android:name=".IDS.AccountMethodsActivity" />
        <activity android:name=".IDS.AuthorizationMethodsActivity" />
        <activity android:name=".IDS.AdminMethodsActivity" />
        <activity android:name=".IDS.AccountsMethodsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.CAMERA" />

</manifest>